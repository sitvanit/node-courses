// inside sql we have data in tables.
// inside noSql we have data in array of objects (which called collection).
// each record called: sql-row/record, noSql-document.
// each column called: sql-column, noSql-field.
// in sql each record should fill all of the column, in mySql each document may have different properties.
